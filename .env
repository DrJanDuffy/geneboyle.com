# REMOVED FROM GIT — secrets were previously committed by mistake.
# Configure all values in the Vercel project Environment Variables dashboard.
# See .env.example for required keys (including FOLLOW_UP_BOSS_* and CLAUDE_API_KEY aliases).
